name: Script check

inputs:
  some:
    description: 'smtng'
    required: true


runs:
  using: "composite"

  steps:

    - uses: actions/github-script@v7
      id: my-script
      with:
        result-encoding: string
        script: |
          console.log('Hello ${{ inputs.some }}')
