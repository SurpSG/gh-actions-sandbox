name: Script check

inputs:
  some:
    description: 'smtng'
    required: true
    default: '<NOT-PROVIDED-ACTOR>'


runs:
  using: "composite"

  steps:

    - uses: actions/github-script@v7
      id: my-script
      with:
        script: |
          console.log('Hello ${{ inputs.some }}')
